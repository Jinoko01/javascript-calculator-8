# 리팩토링 사항

## 1. 덧셈 연산에 전략 패턴 적용

OperatorStrategy 클래스를 정의하여, 사용자가 덧셈 / 뺄셈 / 곱셈 / 나눗셈 중 하나를 수행하고자 했을 때, 클라이언트는 AddOperatorStrategy와 같은 구현체가 아닌 OperatorStrategy를 의존하도록 한다. 이를 통해, 새로운 연산 전략이 추가되었을 때, 새로운 연산 전략 클래스를 추가하는 방식으로 유지보수성을 향상시킬 수 있다.

### Before

```javascript
// 다음과 같이 덧셈 연산만을 위한 클래스를 정의
export default class AddOperator {
  add(nums) {
    return nums.reduce((acc, num) => acc + num, 0);
  }
}

// 만약 덧셈이 아닌 다른 연산을 수행해야 하는 경우 클라이언트 코드도 변경해야 한다.
const operator = new AddOperator();
operator.add([1, 2, 3]);
```

### After

```javascript
// Operator.js
import { ERROR_MESSAGE } from '../data/messages.js';

export default class OperatorStrategy {
  // 타입스크립트가 아닌 이슈로 operate메소드를 구현하지 않으면 자체적으로 에러를 발생시키도록 함
  operate(nums) {
    new Error(ERROR_MESSAGE.NOT_IMPLEMENT_OPERATOR_METHOD);
  }
}
```

```javascript
// AddOperator.js
import OperatorStrategy from './Operator.js';

export default class AddOperator extends OperatorStrategy {
  // OperatorStrategy를 상속받아서 해당 클래스에 맞는 연산 전략을 구현
  operate(nums) {
    return nums.reduce((acc, num) => acc + num, 0);
  }
}
```

```javascript
// Calculator.js
import AddOperator from './AddOperator.js';
import Parser from './Parser.js';

export default class Calculator {
  constructor() {
    this.parser = new Parser();
    this.setOperator();
  }

  calculate(input) {
    const arr = this.parse(input);
    return this.operate(arr);
  }

  parse(input) {
    return this.parser.parse(input);
  }

  operate(nums) {
    return this.operator.operate(nums);
  }

  setOperator() {
    // 만약 여러 연산이 추가될 경우, 요구사항에 따른 operator 세터 메소드 구현
    this.operator = new AddOperator();
  }
}
```

### 2. MVC 패턴으로 분리

기존에는 entities 디렉토리 내부에 모든 객체를 모아놓은 채로 구현했다. 이를 MVC 패턴으로 리팩토링하여 사용자 입력을 받는 View, 사용자 입력 데이터를 분석하는 Controller, 이에 적절한 작업을 수행하는 Model로 분리했다.

```
기존 프로젝트 구조
src/
├── data/
│   ├── messages.js
│   └── regex.js
├── entities/
│   ├── AddOperator.js
│   ├── Calculator.js
│   ├── DelimiterSetter.js
│   ├── InputProcessor.js
│   ├── Parser.js
│   └── Splitter.js
├── App.js
└── index.js
```

```
리팩토링 후 프로젝트 구조
src/
├── App.js
├── index.js
├── controller/
│   └── CalculatorController.js
├── data/
│   ├── messages.js
│   └── regex.js
├── model/
│   ├── AddOperator.js
│   ├── Calculator.js
│   ├── DelimiterSetter.js
│   ├── Operator.js
│   ├── Parser.js
│   └── Splitter.js
└── view/
    ├── InputView.js
    └── OutputView.js
```
